# 하이힐 작업 분리 안내

이 폴더의 파일은 하이힐 거래처 상품 이미지 생성 작업용입니다.

현재 상태: 쇼룸 업로드 금지 / 내부 생성 테스트용

- `products.json`에 등록하지 않습니다.
- `assets/` 폴더로 옮기지 않습니다.
- 나이스쇼룸 실제 업로드에 사용하지 않습니다.
- 업로드가 필요해지는 시점에는 별도 승인 후, 최종 검수본만 별도 폴더로 복사합니다.

생성 매니페스트의 `upload_status` 값이 `HOLD_DO_NOT_UPLOAD`인 동안에는 업로드 대상이 아닙니다.
